import { connect } from "react-redux";
import CreateReceipeOrganism from "../../organisms/CreateReceipe";

const mapStateToProps = (state: any) => ({});

const mapDispatchToProps = (dispatch: any) => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(CreateReceipeOrganism);
