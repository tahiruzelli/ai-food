import { connect } from "react-redux";
import AppOrganism from "../../organisms/App";

const mapStateToProps = (state: any) => ({});

const mapDispatchToProps = (dispatch: any) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(AppOrganism);
